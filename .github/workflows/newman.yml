name: Newman Contract Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  newman:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Checkout Core Business repository
        uses: actions/checkout@v4
        with:
          repository: Connectivity-services-ad-PT/lab-3-leanhtg1102
          path: core
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: |
          npm install
          cd core && npm install && cd ..

      - name: Lint OpenAPI contracts
        run: npm run lint:contracts

      - name: Start Prism mock server and run Newman contract tests
        run: |
          npx prism mock contracts/iot-ingestion.openapi.yaml -p 4010 --host 127.0.0.1 > prism-iot.log 2>&1 &
          PRISM_PID=$!
          npx prism mock core/contracts/team-core.openapi.yaml -p 4016 --host 127.0.0.1 > core-business.log 2>&1 &
          CORE_PID=$!
          sleep 5
          echo "=== prism process ==="
          ps -ef | grep prism | grep -v grep || true
          echo "=== prism log after start ==="
          cat prism-iot.log || true
          echo "=== core business mock log after start ==="
          cat core-business.log || true
          echo "=== health check ==="
          timeout=120
          until curl -sSf http://127.0.0.1:4010/health >/dev/null 2>&1 && curl -sSf http://127.0.0.1:4016/health >/dev/null 2>&1; do
            timeout=$((timeout - 1))
            if [ "$timeout" -le 0 ]; then
              echo "Mock health check failed after 120 seconds"
              cat prism-iot.log || true
              cat core-business.log || true
              exit 1
            fi
            sleep 1
          done
          npm run test:mock
          kill "$PRISM_PID" "$CORE_PID" || true

      - name: Show Prism logs on failure
        if: failure()
        run: |
          cat prism-iot.log || true

      - name: Upload Newman reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: newman-reports
          path: reports/
